{
  "dependencies": {
    "fast-xml-parser": "^5.2.5"
  },
  "name": "logos-notes-export",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "export": "bun run index.ts export",
    "process": "bun run index.ts process",
    "markdown": "bun run index.ts markdown",
    "stats": "bun run index.ts stats",
    "help": "bun run index.ts --help",
    "export:range": "bun run index.ts export --from-id",
    "dev": "bun run src/cli.ts"
  }
}
